# Install new dependencies first
pip install -r requirements.txt

# Check service status
python cli.py status

# Launch LangGraph dev server
python cli.py langgraph

# Launch Agent Inbox UI
python cli.py inbox

# Create and test email workflow
python cli.py email

# Get help for any command
python cli.py --help
python cli.py email --help

# Fetch latest Gmail emails and trigger workflow
python cli.py gmail                     # Fetch 1 latest email and send to workflow
python cli.py gmail --count 5           # Fetch 5 latest emails
python cli.py gmail --no-process        # Fetch without triggering workflow
python cli.py gmail --no-body           # Fetch without showing body

# Automated Gmail polling with cronjob (runs every 5 minutes automatically)
python gmail_auto_poller.py               # Test the auto poller manually
crontab -e                                # Edit cronjob (add the line below)
crontab -l                                # List active cronjobs
# Cronjob line to add: */5 * * * * /Users/samuelaudette/Documents/code_projects/agent_inbox_1.12/run_gmail_poller.sh >/dev/null 2>&1
tail -f gmail_poller.log                  # Monitor auto poller activity
cat processed_emails.json                 # See which emails were processed

# venv
source .venv/bin/activate
